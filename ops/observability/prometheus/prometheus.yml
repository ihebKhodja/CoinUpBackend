global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]

  # Container-level metrics
  - job_name: "cadvisor"
    static_configs:
      - targets: ["cadvisor:8080"]

rule_files:
  - /etc/prometheus/alerts.yml
